#!/bin/bash

URL=https://raw.githubusercontent.com/horta/dotfiles/main

/bin/bash -c "$(curl -fsSL $URL/install-deps)"

cd ~/
git clone https://github.com/horta/dotfiles.git --depth 1
cd dotfiles

./install

source ~/.bashrc
